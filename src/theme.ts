const theme = {
  colors: {
    grey: "#e5e5e5",
    dargGrey: "#808080",
    white: "#ffffff",
    yellow: "#fca311",
    navy: "#14213d",
    black: "#000000",
  },
};

export type ThemeType = typeof theme;
export default theme;
